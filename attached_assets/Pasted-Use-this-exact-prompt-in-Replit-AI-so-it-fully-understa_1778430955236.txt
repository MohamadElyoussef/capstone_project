Use this exact prompt in Replit AI so it fully understands the architecture, backend flow, UI changes, and scheduling behavior you want.

---

# Summer Scheduling System — Full Implementation Prompt

We already have a fully working university scheduling system using:

* Python
* FastAPI
* SQLAlchemy
* OR-Tools CP-SAT
* Existing schedule generation logic
* Existing weekly grid
* Existing doctor schedule views
* Existing CSV import system

The current scheduler works correctly, so DO NOT break or rewrite the current implementation.

We now need to add a completely separate Summer Scheduling system while keeping the rest of the application architecture shared.

---

# VERY IMPORTANT RULES

## DO NOT:

* Modify the existing scheduling algorithm directly
* Break existing views
* Create duplicate schedule grids
* Create duplicate doctor views
* Create separate DB tables for summer

## DO:

* Create a parallel summer scheduling algorithm
* Reuse all existing shared views
* Keep the same ScheduledMeeting table
* Make Summer schedules replace Regular schedules completely

---

# REQUIRED SYSTEM BEHAVIOR

The application should only have ONE active schedule at a time.

Meaning:

* If admin generates a Regular schedule → regular meetings become active
* If admin generates a Summer schedule → summer meetings replace all regular meetings

This is exactly how the current Generate Schedule behavior already works.

---

# HOW THE FLOW SHOULD WORK

## Existing Flow

Current button:

```text id="ow4gq9"
Generate Schedule
```

Current behavior:

1. Clears all ScheduledMeeting records
2. Runs regular scheduling algorithm
3. Writes new meetings to DB
4. Weekly Grid updates automatically
5. Doctor schedules update automatically

---

# NEW SUMMER FLOW

Add a new button:

```text id="9jwlmo"
Generate Summer Schedule
```

When clicked:

1. Clear ALL existing ScheduledMeeting records
2. Run a NEW Summer scheduling algorithm
3. Write generated summer meetings into the SAME ScheduledMeeting table
4. Weekly Grid automatically displays summer schedule
5. Doctor schedules automatically display summer meetings
6. Existing views continue working without modification

---

# UI CHANGES

## Remove Registration Control Section

Remove:

* Open Registration
* Close Registration

Replace entire section with:

# SUMMER

And under it place ONLY ONE button:

```text id="h0v85z"
Generate Summer Schedule
```

Keep all other dashboard components exactly the same:

* Import CSV
* Generate Schedule
* View Unscheduled
* View Suggestions
* View Conflicts
* View Audit Log
* View Doctors

---

# OPTIONAL BUT RECOMMENDED

Add a small dashboard label:

```text id="q8b4zl"
Current Schedule: Summer
```

or

```text id="z4a6lj"
Current Schedule: Regular
```

Store this as:

* A DB setting
* Or simple config value

Update it whenever:

* Generate Schedule is pressed
* Generate Summer Schedule is pressed

This prevents admin confusion.

---

# BACKEND REQUIREMENTS

# 1. CREATE NEW SUMMER ALGORITHM FILE

Create a completely separate algorithm file.

Example:

```python id="h2sll4"
ga_schedule_summer.py
```

This file should:

* Reuse helper utilities safely if needed
* Use the same database models
* Write to ScheduledMeeting
* Use Summer-specific scheduling rules

DO NOT modify the existing:

```python id="6gxoq7"
ga_schedule_sections.py
```

---

# 2. CREATE NEW API ENDPOINT

Inside:

```python id="r5y8yw"
admin_schedule.py
```

Add new endpoint:

```python id="4nb3zq"
POST /admin/schedule/generate-summer
```

Behavior:

1. Delete all ScheduledMeeting rows
2. Call:

```python id="zxh0rl"
ga_schedule_summer.py
```

3. Save generated meetings
4. Return success response
5. Write audit log entry

Example audit log action:

```text id="dj6x7d"
SCHEDULE_GENERATED_SUMMER
```

---

# 3. FRONTEND BUTTON

Add frontend button:

```text id="ll1r9v"
Generate Summer Schedule
```

When clicked:

* Call:

```text id="u6prhk"
/admin/schedule/generate-summer
```

Show:

* Loading state
* Success notification
* Error notification

Same behavior as current Generate Schedule button.

---

# IMPORTANT ARCHITECTURE DESIGN

The Weekly Grid, Doctor Views, Suggestions, Conflicts, and Audit Logs should NOT be duplicated.

All shared components should continue reading from:

```python id="n9wtlh"
ScheduledMeeting
```

Because:

* Summer replaces regular schedule completely
* Only one active schedule exists at a time

This keeps architecture simple and clean.

---

# EXISTING COMPONENTS THAT MUST CONTINUE WORKING

These should work automatically without redesign:

| Component            | Expected Behavior                           |
| -------------------- | ------------------------------------------- |
| Weekly Schedule Grid | Shows whichever schedule was last generated |
| View Doctors         | Shows doctor meetings from current schedule |
| View Conflicts       | Checks current meetings                     |
| Suggestions          | Uses current schedule                       |
| Audit Log            | Records generation action                   |
| Unscheduled Sections | Uses current generated state                |

---

# SUMMER SCHEDULING RULES

All Summer sessions duration:

```text id="1gr5m9"
119 minutes
```

---

# RULE 1 — Lecture Only

If course contains ONLY:

* Lecture

Then:

* Lecture occurs 4 times per week
* Each lecture = 119 minutes

---

# RULE 2 — Lecture + Lab

If course contains:

* Lecture
* Lab

Then:

* Lecture = 3 times weekly
* Lab = 2 times weekly
* Every session = 119 minutes

---

# RULE 3 — Lecture + Tutorial

If course contains:

* Lecture
* Tutorial

Then:

* Lecture = 4 times weekly
* Tutorial = 2 times weekly
* Every session = 119 minutes

---

# RULE 4 — Lecture + Lab + Tutorial

If course contains:

* Lecture
* Lab
* Tutorial

Then:

* Lecture = 3 times weekly
* Lab = 2 times weekly
* Tutorial = 2 times weekly
* Every session = 119 minutes

---

# TBA DOCTOR FEATURE (IMPORTANT)

Current behavior:

* TBA courses only allow time editing

New requirement:

* Allow assigning a real doctor name to TBA courses

Example:

```text id="vn8m3w"
TBA → Mahmoud Al Hammad
```

---

# REQUIRED TBA BEHAVIOR

When admin assigns a doctor:

1. Update instructor field in DB
2. Remove course from TBA list
3. Add course automatically to doctor timetable
4. Refresh doctor schedules dynamically
5. Refresh timetable views automatically

No page reload hacks — use proper state refresh.

---

# IMPORT SYSTEM

Keep current CSV import functionality:

* Rooms CSV
* Courses CSV

Summer scheduling should use the SAME imported data.

No separate import system needed.

---

# CLEAN CODE REQUIREMENTS

## DO:

* Keep Summer logic modular
* Use reusable helper functions
* Keep architecture clean
* Add comments where necessary

## DO NOT:

* Duplicate unnecessary frontend components
* Duplicate database tables
* Duplicate weekly grid pages
* Mix Summer logic into regular scheduler

---

# EXPECTED FINAL RESULT

After implementation:

## Dashboard

* Registration controls removed
* New Summer section added
* Generate Summer Schedule button works

## Backend

* New Summer scheduling algorithm exists
* New API endpoint exists
* Existing scheduler untouched

## Shared Components

* Weekly Grid works normally
* Doctor schedules work normally
* Suggestions/conflicts work normally

## Schedule Behavior

* Only one schedule active at a time
* Summer replaces regular
* Regular replaces summer

## TBA Feature

* Admin can assign doctor names dynamically
* Courses automatically move into doctor schedules

## Architecture

* Clean
* Modular
* Stable
* Maintainable
